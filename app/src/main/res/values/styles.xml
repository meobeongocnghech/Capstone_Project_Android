<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Toolbar">
        <item name="colorControlNormal">@android:color/white</item>
    </style>

    <style name="RegisterTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorDefaultBackground</item>
    </style>

    <style name="SwitchTheme" parent="Theme.MaterialComponents.Light">
        <item name="android:textColor">#fff</item>
        <item name="android:textSize">9sp</item>
    </style>

    <style name="PlaceToolbarTitle" parent="@android:style/TextAppearance.Medium">
        <item name="android:textSize">24sp</item>
    </style>

    <style name="spinner_style">
        <item name="android:dropDownWidth">200dp</item>
        <item name="android:showDividers">none</item>
        <item name="android:dividerHeight">0dp</item>
        <item name="android:dropDownHeight">70dp</item>
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:dropDownSelector">@android:color/white</item>
    </style>

    <style name="TextInputLayoutStyle" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/colorTextFieldLabel</item>
        <item name="android:textSize">11.5sp</item>
    </style>

    <style name="RadioStyle" parent="@style/AppTheme">
        <item name="android:colorControlActivated">@color/colorAccent</item>
    </style>

</resources>
